package de.sandstorm.configdocgen.core.model

enum class HowToFeature {
    /**
     * Shows how to configure the property via Spring conventions.
     * (environment var name and application.yaml reference)
     */
    SPRING,
}
