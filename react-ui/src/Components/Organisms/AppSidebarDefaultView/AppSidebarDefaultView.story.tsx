import {storiesOf} from '@storybook/react';
import * as React from 'react';
import AppSidebarDefaultView from '.';

storiesOf('AppSidebarDefaultView', module)
    .add('default', () => (
        <AppSidebarDefaultView />
    ))
;
